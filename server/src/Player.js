const {row, col, create_2d_array} = require("./utils")

class Player {
	constructor(name) {
		this.name = name;
		// this.room_name = null;
		// this.master = false;
	}
}

module.exports = { Player };